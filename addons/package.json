{
	"name": "macos-spellchecker-addon",
	"description": "N-API addon for the macOS spell-checker",
	"version": "0.1.0",
	"main": "",
	"publishConfig": {
		"access": "restricted"
	},
	"scripts": {
		"build-x64": "node-gyp rebuild -arch x64 --verbose && cp build/Release/*.node ./bin",
		"build-arm64": "node-gyp rebuild -arch arm64 --verbose && cp build/Release/*.node ./bin"
	},
	"dependencies": {},
	"devDependencies": {
		"node-gyp": "^10.2.0",
		"node-addon-api": "^8.2.1"
	},
	"author": "Rotem Dan",
	"license": "MIT"
}
